{
  "name": "earth-visualization",
  "version": "1.0.0",
  "description": "Interactive 3D Earth visualization with realistic effects",
  "type": "module",
  "scripts": {
    "setup": "node setup.js",
    "dev": "vite",
    "build": "node -e \"try { require('fs').mkdirSync('public/assets/earth', { recursive: true }); } catch(e) {}\" && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
